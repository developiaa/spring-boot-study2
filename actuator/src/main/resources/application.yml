management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
#      show-details: always
      show-components: always

## env를 활성화하면 info로 해당하는 부분 노출
info:
  app:
    name: hello-actuatora
    company: developia
logging:
  level:
    hello.actuator.controller: trace
